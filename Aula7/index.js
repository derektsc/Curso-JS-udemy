// não podemos criar constantes com palavras reservadas
// constantes precisam ter nomes significativos
// Não pode começar o nome de uma constante com um número
// Não podem conter espaços ou traços
// Utiliza camelCase (let nomeCompleto='';)
// Case-sensitive ele diferencia maiuscula de minuscula
// não utilize var, utilize CONST.
// sempre precisa iniciar a constante
// não da para alterar o nome da constante depois

const nome = 'João';
console.log(nome); 

// + - * /
const primeiroNumero = 5; //Number
const segundoNumero = 10;
const terceiroNumero ='5';
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2;
let resultadoTriplicado = resultado * 3;
resultadoTriplicado = resultadoTriplicado + 5;
console.log (resultado);
console.log (resultadoDuplicado);
console.log (resultadoTriplicado)
console.log (primeiroNumero + segundoNumero);
console.log (primeiroNumero + terceiroNumero);

console.log(typeof primeiroNumero); //verifica o tipo de variavel



